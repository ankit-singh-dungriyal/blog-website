<%- include("partials/header") -%>



<h1> Home </h1>
<p><%= homeContent %></p>

<% blogs.forEach(function(blog){ %>
  <h2><%= blog.title %></h2>
  <p><%= blog.post.substring(0,200) + "..." %>
    <a href="<%= "/"+lodash.kebabCase(blog.title) %> ">Read more</a></p>
<% }); %>

<%- include("partials/footer") -%>
